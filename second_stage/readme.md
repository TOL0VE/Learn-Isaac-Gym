🛠️ 核心架构图你的 PPO 网络将不再是一个简单的“感知-反应”系统，而是一个带有记忆的系统。$$\text{Observation}_t + \text{Command}_t \rightarrow [\text{FC}] \rightarrow \text{Memory}_{t-1} \rightarrow [\text{LSTM}] \rightarrow \text{Action}_t$$Observation: 车位置 $x$, 车速度 $\dot{x}$, 杆角度 $\theta$, 杆角速度 $\dot{\theta}$。Command: 键盘输入的目标速度 $v_{target}$。Memory: 上一帧的隐藏状态 $(h_{t-1}, c_{t-1})$。